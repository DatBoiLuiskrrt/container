import React from "react";
import Menu from '@material-ui/core/Menu';




const Menus = (props) => {
    return (
        <div>
    <h1>Hello my name is {props.name} {props.lastName}</h1>

        </div>
    )
};

export default Menus;